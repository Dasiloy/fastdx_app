import 'package:flutter/material.dart';

/// Your base design colors (usually from Figma)
class AppColors {
  static const seedLight = Color(0xFFFF6A2E); // richer orange than FF4B3E
  static const seedDark = Color(0xFFFF5B2E); // deeper version for dark mode

  static const secondaryLight = Color(0xFF1E1E2E);
  static const secondaryDark = Color(0xFF2D2D40);

  // Tab
  static const tabColor = Color(0xFFAFAFAF);
  static const tabColorDark = Color(0xFFBDBDBD);

  // Bg
  static const bgLight = Color(0xFFFDFDFD);
  static const bgDark = Color(0xFF121212);

  // border
  static const borderLight = Color(0xFFE8EAED);
  static const borderDark = Color(0xFF1E1E1E);

  // Common colors (you can add as needed)
  static const gray = Color(0xFFE3EBF2);
  static const grayDark = Color(0xFF3C4A55);
  static const labelGray = Color(0xFF646982);
  static const labelGrayDark = Color(0xFFA8ADCC);
  static const container = Color(0xFFFFF1F2);
  static const containerDark = Color(0xFF2B0F14);
  static const success = Color(0xFF00C853);
  static const warning = Color(0xFFFFC107);
  static const error = Color(0xFFD32F2F);
}
